[project]
name = "flask-zenobank-examples"
version = "0.1.0"
description = "Flask Zenobank Examples"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.1",
    "python-dotenv>=1.1",
]

[dependency-groups]
dev = [
    "pytest>=8.0",
]
